/**
 * Workspace Components
 *
 * Components for the AI Workspace feature - an isolated virtual display
 * where the AI can work without hijacking the user's mouse/keyboard.
 */

export { WorkspacePiP } from './WorkspacePiP';
export type { default as WorkspacePiPComponent } from './WorkspacePiP';
